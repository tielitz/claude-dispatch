//! Config schema migration helpers.
//!
//! This module will handle migrating user configs between schema versions
//! once a `schema_version` field is introduced. Implementation arrives in a
//! future task of the layered-config refactor.
